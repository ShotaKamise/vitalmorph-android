# VitaMorph 引き継ぎ状況

最終更新: 2026-07-12

## v0.11 サマリー(完成計画 T1〜T6)

`docs/COMPLETION_PLAN.md` の完成(v1.0)計画に沿って進行中。T1〜T5は完了、T6(本ドキュメント整備)を実施中、T7(リリース準備)が残る。

- T1 モーション6種: 完了。`TOUCH_HAPPY`/`TOUCH_SHY`/`TOUCH_ANNOYED`/`TALK`/`SAD`/`MINIGAME_SUCCESS` をコードの変形アニメーションとして実装しUIへ連携。
- T2 性格システム: 完了(Room v7)。孵化時に5性格を抽選し世代へ永続化。効果は会話トーンと連続タッチ許容回数のみで能力差なし。
- T3 大会進行状態の永続化: 完了。進行中のターン制大会をJSONスナップショットでSharedPreferencesへ保存し、途中から再開可能。
- T4 図鑑: 完了(Room v8)。全71体の図鑑をトレーナータブに追加。出会った姿を発見済みとして永続化、系譜から遡及取り込み。
- T5 レシピ材料の個別保存: 完了(Room v9)。材料付きでレシピを保存・閲覧・削除し、1タップで記録可能。
- T6 ドキュメント整備とロスター更新: 実施中。ロスターを71体構成へ更新、READMEを現機能へ更新、本書を整理。
- T7 v1.0.0リリース準備: 完了。versionCode 12 / versionName 1.0.0。署名済みAPKの公開はタグ(例: v1.0.0)をプッシュするとGitHub Actionsのreleaseワークフローが実行される(署名シークレットが必要なためタグ付けはユーザー操作)。

## 現在の完成範囲

- Androidアプリ基盤
- Health Connectから歩数、活動カロリー、運動時間、栄養を読み取る処理
- あすけんの目標値に合わせたカロリー・PFC目標設定
- 28日シーズン、7日ごとの4段階進化
- 共通7体・動物系36体のモンスター画像(共通の幼生・成長体は♂/♀マーク表示)
- 人型28体(男女、成熟体、最終形態)の画像と正式ID
- 待機、攻撃、被弾、勝利アニメーション
- 技とアイテムを選ぶCPUターン制トーナメント
- 系統固有技、エネルギー、ガード、回復、CPU判断、バトルログ
- 13シーズンと1,100XPのトレーナーランク
- デモモード
- SharedPreferencesによる既存ゲーム状態保存
- GitHub Actionsによるテストとdebug APK生成(main、claude/**、codex/**ブランチで実行)
- タグによる署名済みrelease APK公開ワークフロー
- (v0.4 / Phase 1) Roomデータベース導入(trainer_profile、monster_generation、legacy_stats)
- (v0.4 / Phase 1) トレーナー名の初回設定(ホーム)と変更(設定タブ)、1〜12文字・制御文字禁止の検証
- (v0.4 / Phase 1) 孵化時のオス・メス決定と永続化、ホーム画面での♂/♀表示
- (v0.4 / Phase 1) 機嫌・絆・継承ポイントの保存基盤
- (v0.5 / Phase 2) 人型28体の正式IDを使った性別別進化ルート(第3週=人型成熟体、第4週=上位人型または動物系最終形態)
- (v0.5 / Phase 2) タッチリアクション(部位判定、クールダウン、連続タッチで嫌がる、1日の報酬上限)
- (v0.5 / Phase 2) 機嫌・絆の変動ルール(タッチ、会話、日次の記録有無)とホーム画面のバー表示
- (v0.5 / Phase 2) 端末内テンプレート会話(トレーナー名挿入、時刻・機嫌・絆・活動・大会コンテキスト、選択肢による機嫌絆変化)
- (v0.5 / Phase 2) Room v2移行(interaction_state テーブル追加)
- (v0.6 / Phase 3) ミニゲーム3種(コアキャッチ、パルストレーニング、ミールバランス)。報酬は1日3回まで、プレイ自体は無制限
- (v0.6 / Phase 3) 機嫌のバトル補正(±5%以内: 絶好調=開始シールド、好調=素早さ+5%、不調=素早さ-5%、元気がない=開始エネルギー-1)。機嫌だけで参加は禁止しない
- (v0.6 / Phase 3) 絆60以上で大会中1回の「トレーナーの応援」(HP25+エネルギー1)
- (v0.7 / Phase 4) シーズン完了時の継承ポイント付与(食事・運動・交流・大会から各1pt、1世代3ptまで)
- (v0.7 / Phase 4) 継承ポイントのバトル反映(1pt=+1%、各能力15%上限、減らない)
- (v0.7 / Phase 4) トレーナータブの系譜画面(歴代の性別、最終形態、大会順位、継承内容)
- (v0.7 / Phase 4) 世代記録の拡張(Room v3: finalFormId、finalPlacement、awarded*列)
- (v0.8 / Phase 5) 食事タブ(朝昼夕間の入力、代表食品カタログ約60品目、手入力、自作食品、お気に入り、最近の記録、前日コピー)
- (v0.8 / Phase 5) 日次サマリー(目標との差、PFCバー)と直近7日のエネルギーグラフ
- (v0.8 / Phase 5) Health ConnectへのNutritionRecord書き込み(Client Record ID付き、削除も同期)
- (v0.8 / Phase 5) 栄養データの優先元選択(VitaMorph優先/あすけん優先。合算しない)とRoom v4(food_entry等3テーブル)
- (v0.9 / Phase 6) Open Food Factsの食品検索(初の外部通信。送信は検索語とバーコード番号のみ)
- (v0.9 / Phase 6) バーコードスキャン(CameraX + ML Kitオンデバイス処理、カメラ権限)
- (v0.9 / Phase 6) レシピ登録(複数食品の合計を自作食品として保存)
- (v0.9 / Phase 6) 優先元「日ごとに選択」のUIと日別選択の永続化
- (v0.9 / Phase 6) 代表的なビタミン・ミネラル(VC/Ca/鉄)の記録・表示・HC書き込み(Room v5)
- (v0.10) 71体進化表(docs/EVOLUTION_TABLE.md)。孵化時にルート適性(人型/動物、約50%)を抽選し、動物ルートでは性別割り振り済みの成熟体12体と最終形態24体へ進化する。全71体が入手可能

## 現在のバージョン

- applicationId: `app.vitalmorph`
- versionCode: `12`
- versionName: `1.0.0`
- minSdk: `28`
- targetSdk / compileSdk: `36`
- Room: `2.8.4`(KSP `2.3.9`、スキーマは `app/schemas/` へ出力、DBバージョン6)
- 新規依存(v0.9): CameraX 1.4.1、ML Kit barcode-scanning 17.3.0

## Phase 2の実装メモ

- 新進化表は既存ユーザーの進行中シーズンにも即適用する(2026-07-12ユーザー決定)。再計算で第3週以降の姿が人型に変わる。
- 第1〜2週は動物系の性別差分画像が未制作のため、現行のモルフィ+6系統成長体を共通画像+♂/♀マークで使い続ける(2026-07-12ユーザー決定)。Codexの選抜・差分完成後に差し替える。
- 職業分岐(第2週の傾向、`EvolutionEngine.chooseJob`、2026-07-12ユーザー承認):
  - 調和→剣士 / 筋力・蓄積→大剣使い / 過活動→ショットガン使い
  - 俊足は歩数型(週4日以上歩数目標達成)→二刀流剣士、それ以外→槍使い
  - 静養は記録が規則的(継続性70以上)→魔法使い、不規則→忍者
- 第4週の分岐(`EvolutionEngine.choosesHumanoidFinal`): 第3週の継続性50以上かつ(機嫌60以上または絆40以上)で上位人型。それ以外は職業の系統と性別に応じた動物系最終形態(オス: アステリオン/バスティオンレックス/ゼファリオン、メス: ミラフローラ/ルナヴェルデ/フェニクレスト)。交流しない遊び方でも完成ルートになる設計。
- 旧進化表の成熟体・最終形態のフォーム定義は削除せず `EvolutionEngine.allForms` に保持(公開済みIDは削除しない規約)。
- 機嫌・絆ルール(`MoodEngine`): タッチ+2/+1/-2、絆+1(タッチ1日10回・会話1日5回まで反映)。前日に記録があれば機嫌+3、記録も交流も無ければ-2。絆は減らない。
- タッチ(`InteractionEngine`): 800msクールダウン、5秒以内の連続タッチ7回目から嫌がる。日付の巻き戻しでは1日カウントをリセットしない。
- 会話(`DialogueEngine`)は端末内テンプレートのみ。責めない・命令しない・医療的断定をしない文体。トレーナー名未設定時は「トレーナー」で呼ぶ。
- タッチ・会話・ミニゲーム用の専用モーション6種(`TOUCH_HAPPY`/`TOUCH_SHY`/`TOUCH_ANNOYED`/`TALK`/`SAD`/`MINIGAME_SUCCESS`)を実装済み(v0.11 / T1)。喜び=TOUCH_HAPPY、照れ=TOUCH_SHY、嫌がり=TOUCH_ANNOYEDへ差し替え、会話返答表示中=TALK、機嫌BAD帯=SAD、ミニゲーム成功=MINIGAME_SUCCESSへ連携。

## Phase 3の実装メモ

- ミニゲームの判定・スコア・成否は `MiniGameRules`(純ロジック)、タイミングと描画は `MiniGameScreens.kt`。
- 報酬: 成功=機嫌+4・絆+2、不成功でも挑戦ボーナスで機嫌+1(責めない)。反映は `InteractionEngine.onMiniGame` が1日3回まで管理し、時刻の巻き戻しでは復活しない。
- バトル補正は `BattleEngine.startTournament(mood, bond)`。素早さ補正は±5%(整数丸めで消えないよう最低±1)。応援は `trainer_cheer` アイテムとして大会中1回、ラウンドをまたいで持ち越す。
- ミールバランスの食品と主要栄養素は一般的な代表値による教育的な内容で、栄養指導や診断ではない。

## Phase 4の実装メモ

- 継承ポイントの獲得条件(`LegacyAwardEngine`): 絆40以上→HP+1、大会ポイント4以上→攻撃+1、シーズン栄養スコア60以上→防御+1、活動スコア70以上か歩数目標12日以上→素早さ+1。
- 4候補すべて達成した場合は `LegacyStats.addingGeneration` がHP→攻撃→防御→素早さの優先順で3ptに切り詰める。世代レコードには上限適用後の実付与量を保存する。
- バトル反映は `BattleEngine.startTournament(legacy)` で1pt=+1%(整数演算 `base*(100+pt)/100`)。機嫌補正と合成される。
- 大会順位は結果画面を閉じた後でも記録できるよう、保存済み大会ポイントから `placementForPoints` で復元する。
- 新世代は `MonsterGeneration` のデフォルト値で孵化するため、機嫌50・絆0への初期化は自動で満たされる。

## Phase 5の実装メモ

- 優先元の既定は「VitaMorph優先」(2026-07-12ユーザー決定)。設定タブで「あすけん優先」へ変更可能。異なるアプリの記録は合算しない(`NutritionResolver`)。
- 二重計上対策: VitaMorphが書き込むNutritionRecordにはClient Record ID(`vitalmorph-food-<UUID>`)を付け、読み取り時は自パッケージのData Originを除外する(`HealthConnectRepository.readDays`)。
- 栄養の読み取りを集計APIからレコード単位に変更した(Origin別に分けるため)。歩数・活動カロリーは従来どおり集計APIを使用。
- 代表食品カタログ(約60品目)は成分表(八訂)を基にした1食分の概算値。出典表記は設定タブとコード(`FoodCatalog.ATTRIBUTION`)。全量(約2,500品目)の導入はPhase 6で判断。
- WRITE_NUTRITION権限を追加したため、既存ユーザーは初回に権限の再許可が必要(ホームのカードから許可できる)。
- Health Connect未接続でも食事記録は動作し、育成へ反映される(`NutritionResolver.mergeDays` がローカル記録だけで日次データを構築)。
- デモモードはデモデータをそのまま使い、食事記録のマージとHC書き込みを行わない。
- 「日ごとに選択」(SELECT_PER_DAY)はenum定義のみで、UIはPhase 6で提供。現状はVitaMorph優先と同じ挙動。

## Phase 6の実装メモ

- 外部通信は2026-07-12にユーザー承認済み。Open Food Facts(world.openfoodfacts.org)のみで、送信するのは検索キーワードまたはバーコード番号だけ。健康データは送信しない。設定タブと食事タブにプライバシー説明を記載。
- 通信は `OpenFoodFactsRepository`(HttpURLConnection、タイムアウト8秒、専用User-Agent)。JSON解析は `OpenFoodFactsParser` で純関数化しテスト済み。オフライン時・失敗時は同梱カタログと手入力で従来どおり動く。
- 外部検索結果は「確認して記録」フローを必須にし、そのまま自動記録しない(結果確認と手動修正の要件)。
- バーコードはML Kitオンデバイス処理(EAN-13/8、UPC-A/E)。カメラ画像は端末外へ出ない。カメラ権限は任意機能(`uses-feature required=false`)。
- レシピは複数食品の合計値を1食の自作食品として保存する簡易版。構成材料の個別保存は将来の拡張。
- ビタミン・ミネラルは代表3種(ビタミンC・カルシウム・鉄)のみ。目標値は設けず参考表示に留める(責めない設計)。値が0の食品(同梱カタログ等)は表示に含まれない。
- 「日ごとに選択」は日付ごとの選択をSharedPreferencesへ保存。未選択の日は記録した側を尊重する。

## v0.11の実装メモ

- タッチ・会話・ミニゲーム用の専用モーション6種を実装(T1、上記Phase 2メモ参照)。
- 性格システム(T2、DBバージョン7): 孵化時に5性格(がんばりや/のんびり/クール/あまえんぼう/きまぐれ)を均等抽選し、`MonsterGeneration.personality` として世代へ永続化する。同じ世代では変更しない。
  - 割り当て(`PersonalityAssigner`): 新規孵化(履歴あり・シーズン切替時)は乱数で均等抽選。既存ユーザーの初回移行(履歴なし)は保存済みseasonStartと固定ソルトから決定的に求め、起動ごとに再抽選しない。マイグレーションで追加された既存の進行中世代はHARDWORKER(がんばりや)が既定のため、シーズン途中で見た目や挙動が急変しない。
  - 効果は会話トーンと連続タッチ許容回数のみ。**能力差・バトル補正は一切付けない**(規約)。
    - 会話(`DialogueEngine`): 性格ごとに2文以上の追加セリフ。がんばりや=前向きで熱血、のんびり=ゆったり癒し系、クール=そっけないが根は優しい、あまえんぼう=甘えた口調、きまぐれ=気分屋。責めない・命令しない・医療的断定をしない文体は維持。
    - タッチ(`InteractionEngine.rapidTouchLimitFor`): 連続タッチ許容回数をあまえんぼう+2・クール-2・その他±0。報酬の1日上限は変更しない。
  - ホーム画面と系譜カードに「せいかく: 〇〇」を表示。
- 大会進行状態の永続化(T3): 進行中のターン制大会(`TurnBattleState`)をJSONスナップショットとしてSharedPreferences(`battle_state`)へ保存し、アプリ終了後も試合途中から再開できるようにした。
  - シリアライズは純ロジック `BattleStateCodec`(org.json、全フィールドを保存、往復でデータクラス等価)。バージョン付きで、不一致・破損データは復元せず安全に破棄する(nullを返しアプリを落とさない)。
  - 保存タイミング: 各バトル操作後(開始・技・アイテム・次ラウンド)。消去: 大会終了(優勝・敗退)・シーズン完了・全リセット時。
  - 復元: `refresh` 時に `state.battle` がnullで、オンボーディング完了かつシーズン継続中(28日目まで)のときのみ復元する。一時的な進行状態のため `StoredGameState`/`load()` には含めない。
- 図鑑(T4、DBバージョン8): 全71体を図鑑としてトレーナータブに表示し、出会った(その姿になった)フォームを発見済みとして永続化する。
  - 発見記録の仕組み: `discovered_form` テーブル(`formId` 主キー、`firstSeenAt`、`generationNumber`)。DAOはIGNORE挿入で初回発見だけを残す。`refresh` で現在の進化経路(`evolution.path`)をその世代番号で記録する。記録失敗は空集合で握りつぶし、育成表示を止めない。
  - 遡及取り込み: 既存ユーザーの系譜(過去世代の `finalFormId`)を、古い世代から順に発見済みへ取り込む。初回起動時に自動で図鑑が埋まる。
  - 表示カタログは純ロジック `DexCatalog`(`domain/DexCatalog.kt`、単体テスト済み)。共通7体→人型ルート28体(♂→♀)→動物ルート36体(♂→♀、`EvolutionEngine.animalFormSex` で割り振り)の3セクションで並べ、UIは4列のラップグリッドで描画する。
  - 未発見セルは同じ画像を黒塗りシルエット(`ColorFilter.tint(Color.Black)`)+「???」で表示。`MonsterArtwork.kt` は変更しない。発見済みには名前と♂/♀マーク(共通は無印)を表示する。
- レシピの材料個別保存(T5、DBバージョン9): よく食べる組み合わせを材料付きで保存・閲覧・削除できるようにした。
  - `recipe`(見出し: `recipeId`/`name`/`createdAt`)と `recipe_item`(材料: `recipeId` でひも付け、数量換算後の栄養を保持)の2テーブル。外部キー制約は張らず、削除時にDAOで両表を消す。合計は材料の総和として算出する。
  - レシピ保存時に合計を「1食」の自作食品としても同時に保存し、従来どおりカタログから1タップ記録もできる(既存挙動の維持)。
  - 保存したレシピは食事タブの「保存したレシピ」カードに一覧表示。材料の展開表示、朝/昼/夕/間食の区分を選んで合計栄養を1タップ記録、削除に対応。
  - レシピ組み立ては純ロジック `RecipeFactory.fromParts`(`domain/MealModels.kt`、単体テスト済み)。各材料を `FoodCatalogItem.scaledTo` で数量換算して `RecipeItem` にする。

## まだ実装していないもの

- 成分表全量(約2,500品目)の同梱

## 現在の既知の制約

- 栄養データはHealth Connectからの読み取りのみ。
- 第1〜2週の動物系(幼生・成長体)は共通画像+♂/♀マーク表示を正式仕様とする(性別差分画像の新規生成は行わない、COMPLETION_PLAN 対象外)。
- 動物ルートの成熟体12種・最終形態24種は動物ルートで実際に入手でき、全71体が図鑑(T4)に表示される。
- Roomのマイグレーション・DAOの計装テスト(androidTest)は未追加。v1→v2移行はテーブル追加のみで既存データに触れない。ドメイン層はJVM単体テストで検証済み。
- 実機接続による最終操作確認は未実施。ローカルで `test` と `assembleDebug` は成功済み。

## Claudeが次に行う作業

`docs/IMPLEMENTATION_PLAN.md` のPhase 1〜6、および `docs/COMPLETION_PLAN.md` のT1〜T5はすべて実装完了。以降の作業:

1. T7 リリース準備: versionName/versionCodeを1.0.0 / 12へバンプし、デモモードでの4週進化サイクル確認手順をREADMEへ記載(署名済みAPKのタグ公開ワークフローは構築済み。タグ付けはユーザー操作)。
2. 実機での動作確認とフィードバック反映(バーコード・外部検索・HC書き込みは実機でのみ最終確認可能)。
3. 積み残し: 成分表全量(約2,500品目)の同梱(`docs/COMPLETION_PLAN.md` 対象外、要望があれば再検討)。

新しい大きな機能はユーザーと相談して決める。

## v0.10(71体進化表)の実装メモ

- 詳細は `docs/EVOLUTION_TABLE.md`。動物系の性別割り振り・ルート抽選とも2026-07-12ユーザー承認。
- 既存の進行中世代はDB移行(v6)で人型ルート固定になり、アップデートで姿が変わらない。次の孵化から抽選開始。
- 動物ルートの第4週分岐条件は旧進化表(Phase 1以前)の成熟体別条件を復元して使用。
- `docs/MONSTER_ROSTER.md` は当初43枠構成の記述だったが、T6(2026-07-12)で71体構成へ更新済み。ID・名前・外見仕様は不変。

## 体制

- 2026-07-12以降は全作業をClaudeが担当する(ユーザー決定。旧Codex担当分を引き取り)。以降のCodexへの依頼はない。
- 幼生・成長体の性別差分は共通画像+♂/♀マークの表示を正式仕様とする(性別差分画像の新規生成は行わない)。
- ロスター文書(`docs/MONSTER_ROSTER.md`)は71体構成(`docs/EVOLUTION_TABLE.md`)へ更新済み(T6)。ID・名前・外見仕様は不変。
- 過去のCodex制作物は統合済み。人型28体のデザイン改訂はマージ済みで、職業テーマカラー(剣士=赤など)をアクセント色へ反映し、全形態の画像対応を単体テスト(`MonsterArtworkCoverageTest`)で常時検証している。
- 画像仕様とID規則は `docs/MONSTER_ASSET_CONTRACT.md` のまま変更しない。
